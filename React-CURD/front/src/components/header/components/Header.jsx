import React from 'react';
import { UserHeader, HeaderTitle } from './style';

const Header = () => {
  return (
    <UserHeader>
      <HeaderTitle>ユーザー管理</HeaderTitle>
    </UserHeader>
  );
};

export default Header;
